#This program is to provide a binary to hex, hex to binary and to decimal, etc. It will also allow for adding, subtracting and getting two's complement.

def main():






def decToBinary(decimal):
    for num in decimal:
        if num not 0:
            num = num / 2

